from kivy.lang import Builder
from kivymd.app import MDApp
from kivy.core.text import LabelBase
from kivymd.uix.screenmanager import MDScreenManager
from kivy.core.window import Window

Window.size = (310, 500)


class Example(MDApp):
    def build(self):
        screen_manager = MDScreenManager()
        mn = Builder.load_file("main.kv")
        lg = Builder.load_file("login.kv")
        su = Builder.load_file("signup.kv")
        screen_manager.add_widget(mn)
        screen_manager.real_add_widget(lg)

        return screen_manager


Example().run()
 MDLabel:
            text: "or"
            pos_hint: {"center_y": .22}
            color: rgba(52, 0, 231, 255)
            font_size: "13sp"
            font_name: "BPoppins"
            halign: "center"
        MDFloatLayout:
            md_bg_color: rgba(178, 178, 178, 255)
            pos_hint: {"center_x": .3, "center_y": 218}
            size_hint: .3, .002
        MDFloatLayout:
            md_bg_color: rgba(178, 178, 178, 255)
            pos_hint: {"center_x": .7, "center_y": 218}
            size_hint: .3, .002